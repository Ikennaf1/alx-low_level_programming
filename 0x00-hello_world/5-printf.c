#include <stdio.h>
/**
 * main - This prints specified text to stdout using printf function
 * Return: Returns 0 on success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
