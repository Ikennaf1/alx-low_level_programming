#include "main.h"

/**
 * add - Adds the two integers supplied to function
 * @x: First operand
 * @y: second operand
 * Return: Returns sum of the 2 arguments
 */
int add(int x, int y)
{
	return (x + y);
}
